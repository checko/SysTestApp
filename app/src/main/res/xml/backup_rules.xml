<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude specific files or directories from backup.
         While you can use the <include> tag to include specific files or directories,
         the <exclude> tag is more commonly used to exclude files or directories
         that you don't want to backup. -->
    <exclude domain="sharedpref" path="."/>
</full-backup-content> 